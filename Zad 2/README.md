# PG_RAI_Proj_extra

prepare server and install dependencies:<br>
```
npm init -y
npm install express fs.promises swagger-jsdoc swagger-ui-express mocha
```

run server:
```
node app.js
```

in web browser: http://localhost:3000/api-docs to see documentation in swagger

to run tests in postman: